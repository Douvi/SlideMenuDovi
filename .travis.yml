sudo: false
language: objective-c
osx_image: xcode7.3

before_install:
  - gem install bundler
  - pod install --project-directory=Example

script:
  - xcodebuild -configuration Debug -workspace Example/SlideMenuDovi.xcworkspace -scheme SlideMenuDovi -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 6S,OS=9.3' build test | xcpretty

after_success: slather

# language: objective-c
# osx_image: xcode7.3
#
# script: ./run_test.sh
